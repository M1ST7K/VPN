# Phase 3.0 — Premium Android Experience

Canonical product scope: `docs/HOTFOX_ROADMAP.md` heading «2.9 Premium Android Experience» (renumbered to 3.0 after the owner 2.9 VPN-recovery override).

## Objective

Make HotFox feel like a finished modern Android product without sacrificing editorial visual direction or the truthful VPN state model.

## Binding constraints

- `CONNECTED` / `Защищено` / notification protected / QS ACTIVE only from canonical verified session;
- no fake ping/health/traffic;
- backend-authoritative entitlement;
- no secrets in UI, logs, or notifications;
- do not weaken TUN → HEV → SOCKS `10808` → Xray.

## Repository implementation (ENGINEERING COMPLETE)

- Connection headlines: `Отключено` → `Подбираем маршрут` → `Подключение` → `Проверяем защиту` → `Защищено` via `ConnectionUiMapper`.
- Error presentation: `ConnectionErrorUiMapper` (title/detail/action; diagnostic codes stay in details; secrets stripped).
- Foreground notification and Quick Settings Tile adapters (`NotificationUiMapper` / `QsTileUiMapper`).
- First-run onboarding: welcome → VPN permission → access → AUTO → first connection (`HotfoxOnboardingFlow`).
- Dedicated Autopilot and Routing/privacy surfaces.
- Reduced-motion gating and restrained connect haptics.
- Deterministic ViewState fixtures (`HotfoxPremiumViewState`) as screenshot/golden substitutes.
- `android.app.NotificationManager` aliased as `AndroidNotificationManager`.

Trusted checkpoint **round 19** returned `APPROVED` (P0=0, P1=0) on SHA `21f341980121a49f103bd61eaa588c37f1987c8a`.

## Engineering-exit wording after approval

`3.0 ENGINEERING COMPLETE — runtime and physical release validation deferred.`

Closed. Next phase is `3.1 Mature HotFox Platform / Pre-release Engineering`. Runtime and physical VPN E2E stay **NOT EXECUTED / deferred** until the final release validation gate. Do not claim `RELEASE READY`.
