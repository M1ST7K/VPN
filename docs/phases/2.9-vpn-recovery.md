# Phase 2.9 — VPN Core Recovery

Canonical specification: `docs/HOTFOX_2_9_VPN_RECOVERY.md`.
Validation timing: `.cursor/rules/22-hotfox-owner-release-validation-gate.mdc`.

Objective: implement and harden VpnService → TUN → HEV → SOCKS `127.0.0.1:10808` → Xray, with SOCKS-only isolation before TUN/HEV.

Status: **ENGINEERING COMPLETE — runtime and physical release validation deferred** after checkpoint round 17 (`APPROVED`, P0=0, P1=0) on SHA `4524207` / IPv6 fix `f8de16e` / isolation implementation `dacfe38`.

Runtime and physical VPN E2E stay **NOT EXECUTED / deferred** until the final release validation gate. Do not claim PASS for unrun E2E. Do not claim `RELEASE READY`.
