# Cursor Cloud Agent setup

Use branch `cursor/hotfox-2.2.0-setup` until this bootstrap PR is merged.

Cursor first action:

```text
Read AGENTS.md completely. If V2rayNG/ is absent, run bash bootstrap/bootstrap_source.sh. Then read docs/CURSOR_PRO_ULTRA_MASTER_PROMPT_HOTFOX_PROXY_2.2.0.txt completely, inspect docs/HOTFOX_UI_REFERENCE.jpeg, and execute the specification directly against the Android project. Do not stop at planning.
```

The repository intentionally excludes private signing keys, credentials, personal subscription URLs and the large upstream Xray AAR.
