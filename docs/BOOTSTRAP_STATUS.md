# Bootstrap status

The repository is being prepared for Cursor Cloud Agents.

Required control files are present. The bootstrap payload is versioned as ordered base64 chunks under `bootstrap/payload/`. The branch remains a setup branch until the full verified payload is committed and checked.
