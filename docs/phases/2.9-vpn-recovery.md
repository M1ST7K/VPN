# Phase 2.9 — VPN Core Recovery

Canonical specification: `docs/HOTFOX_2_9_VPN_RECOVERY.md`.

Objective: prove real Internet through VpnService → TUN → HEV → SOCKS `127.0.0.1:10808` → Xray. Isolation starts with Xray SOCKS-only, without TUN/HEV.

Physical handset validation is not this engineering gate; available emulator/runtime E2E is required.
